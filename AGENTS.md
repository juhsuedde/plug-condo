# PlugCondo — Agent Instructions

## Project Overview
React 19 + TypeScript + Supabase app for EV charging management in Brazilian condominiums. Generated by Lovable, deployed on Vercel.

## Commands
- `npm run dev` — Start dev server
- `npm run build` — Production build
- `npm run lint` — Run ESLint
- `npm run format` — Format with Prettier

## Architecture
- **Routing**: TanStack Router with file-based routing in `src/routes/`
- **Layout**: `_authenticated.tsx` gates access; redirects to `/pendente` if `perfis.status_aprovacao != 'aprovado'`
- **State**: TanStack Query + React Query; `useAuth` provides `user`, `perfil`, `loading`
- **Supabase Client**: `src/integrations/supabase/client.ts`

## Env Variables (see .env file)
```
VITE_SUPABASE_URL=https://kfbctthtanxtbqebuwmp.supabase.co
VITE_SUPABASE_ANON_KEY=<from Supabase Dashboard>
SUPABASE_SERVICE_ROLE_KEY=<from Supabase Dashboard>
MERCADO_PAGO_ACCESS_TOKEN=<from Mercado Pago Dashboard>
MERCADO_PAGO_WEBHOOK_SECRET=<from Mercado Pago Dashboard>
```

## Edge Functions (Deno)
| Function | Purpose |
|----------|---------|
| `create-pix-payment` | Creates Pix QR via Mercado Pago |
| `mercado-pago-webhook` | Handles payment status webhooks |
| `process-queue` | Notifies next person in queue |
| `calculate-reservation-cost` | Calculates kWh consumed cost |
| `check-reservation-grace-period` | Cancels reservations not started within 15min |
| `confirm-queue-reservation` | Confirms reservation from queue |

## Key Files
- `src/lib/auth.tsx` — Auth context with perfil fetching
- `src/routes/_authenticated/pendente.tsx` — Pending approval screen
- `src/routes/_authenticated/notificacoes.tsx` — Notifications with filters and pull-to-refresh

## Common Pitfalls
- Edge Functions use Deno runtime (not Node). Use `fetch`, not `axios`
- Supabase client in Edge Functions: `https://esm.sh/@supabase/supabase-js@2`
- Payment webhooks must return 200 even on errors (to prevent MP retries)
- Approval flow: `perfis.status_aprovacao` can be `pendente`, `aprovado`, or `rejeitado`